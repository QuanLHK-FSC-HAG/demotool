import Link from "next/link"; import { ArrowRight, Clock3 } from "lucide-react"; import type { ContentMeta } from "@/lib/content";
export function ContentCard({ item, base, index = 0 }: { item: ContentMeta; base: string; index?: number }) { return <Link href={`${base}/${item.slug}`} className={`content-card tone-${item.color ?? ["blue", "green", "amber", "pink"][index % 4]}`}><div className="card-visual"><span>{String(index + 1).padStart(2, "0")}</span></div><div className="card-body"><div className="meta-row">{item.duration && <span><Clock3 size={15}/>{item.duration}</span>}{item.level && <span>{item.level}</span>}</div><h3>{item.title}</h3><p>{item.description}</p><span className="text-link">Khám phá <ArrowRight size={16}/></span></div></Link> }
