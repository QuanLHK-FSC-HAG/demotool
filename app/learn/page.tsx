import type { Metadata } from "next"; import { BookOpenCheck, Route } from "lucide-react"; import { ContentCard } from "@/components/content-card"; import { getContentList } from "@/lib/content";
export const metadata: Metadata = { title: "Khóa học", description: "Lộ trình Google AI Studio thực hành dành cho giáo viên." };
export default function LearnPage() { const items = getContentList("lessons"); return <><section className="page-hero compact"><div className="container"><span className="eyebrow"><Route/> Lộ trình 7 chặng</span><h1>Học Google AI Studio bằng cách <span>tạo sản phẩm.</span></h1><p>Mỗi bài học giải quyết một việc cụ thể. Đi hết lộ trình, bạn có thể tự tạo, sửa lỗi và chia sẻ game học tập.</p></div></section><section className="section"><div className="container"><div className="learning-banner"><BookOpenCheck/><div><b>Gợi ý cho người mới</b><p>Học theo thứ tự và thực hành ngay với một bài đang dạy. Tổng thời lượng khoảng 90 phút.</p></div></div><div className="card-grid three">{items.map((x,i) => <ContentCard item={x} base="/learn" index={i} key={x.slug}/>)}</div></div></section></> }
