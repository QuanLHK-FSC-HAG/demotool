import type { Metadata } from "next"; import { Gamepad2 } from "lucide-react"; import { ContentCard } from "@/components/content-card"; import { getContentList } from "@/lib/content";
export const metadata: Metadata = { title: "Thư viện game", description: "Chọn cấu trúc game phù hợp với mục tiêu bài học." };
export default function GamesPage() { const items = getContentList("games"); return <><section className="page-hero compact games-hero"><div className="container"><span className="eyebrow"><Gamepad2/> 10 cấu trúc sẵn sàng</span><h1>Mỗi mục tiêu có một <span>cách chơi phù hợp.</span></h1><p>Khám phá khi nào nên dùng từng dạng game, prompt mẫu và mẹo để hoạt động hiệu quả trong lớp.</p></div></section><section className="section"><div className="container card-grid three">{items.map((x,i) => <ContentCard item={x} base="/games" index={i} key={x.slug}/>)}</div></section></> }
