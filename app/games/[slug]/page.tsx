import Link from "next/link"; import { notFound } from "next/navigation"; import { ArrowLeft, WandSparkles } from "lucide-react"; import { MdxContent } from "@/components/mdx-content"; import { getContentItem, getContentList } from "@/lib/content";
export function generateStaticParams() { return getContentList("games").map(({ slug }) => ({ slug })); }
export default async function GamePage({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const item = getContentItem("games", slug); if (!item) notFound(); return <><section className={`article-hero game-detail tone-${item.meta.color}`}><div className="narrow"><Link className="back-link" href="/games"><ArrowLeft/> Thư viện game</Link><span className="eyebrow">Mẫu hoạt động</span><h1>{item.meta.title}</h1><p>{item.meta.description}</p><Link className="button" href={`/builder?game=${slug}`}><WandSparkles/> Tạo prompt dạng này</Link></div></section><article className="article-shell narrow"><MdxContent source={item.content}/></article></> }
