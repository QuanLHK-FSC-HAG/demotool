import type { Metadata } from "next"; import { ExternalLink, GalleryHorizontalEnd } from "lucide-react"; import { getContentList } from "@/lib/content";
export const metadata: Metadata = { title: "Sản phẩm mẫu" };
export default function GalleryPage() { const items = getContentList("gallery"); return <><section className="page-hero compact gallery-hero"><div className="container"><span className="eyebrow"><GalleryHorizontalEnd/> Góc cảm hứng</span><h1>Sản phẩm thật cho <span>lớp học thật.</span></h1><p>Những ý tưởng có thể tái sử dụng, điều chỉnh và biến thành phiên bản của riêng bạn.</p></div></section><section className="section"><div className="container gallery-grid">{items.map((x,i) => <article className={`gallery-card tone-${x.color}`} key={x.slug}><div className="gallery-preview"><span className="preview-emoji">{["🌍","🔢","📚","🧬","🏛️","🔤"][i%6]}</span><div className="preview-ui"><i/><i/><i/></div></div><div className="card-body"><div className="meta-row"><span>{x.level}</span><span>{x.subject}</span></div><h3>{x.title}</h3><p>{x.description}</p><span className="text-link">Xem ý tưởng <ExternalLink/></span></div></article>)}</div></section></> }
